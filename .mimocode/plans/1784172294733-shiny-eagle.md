# Plan: Create Junie Skill for YLx Dev Assistant MCP

## Goal
Create a skill at `~/.junie/skills/ylx-mcp/SKILL.md` that teaches Junie how to use the YLx Dev Assistant MCP server tools.

## Skill Structure

### File: `~/.junie/skills/ylx-mcp/SKILL.md`

**Frontmatter:**
- `name`: ylx-mcp
- `description`: Use when querying YLx project data, reviewing PRs, checking Vercel deployments, auditing code, suggesting features, or querying Sanity data via the YLx Dev Assistant MCP server

**Body sections:**
1. **Overview** — What the YLx Dev Assistant MCP server is, URL, auth
2. **When to Use** — Symptoms/triggers for using this skill
3. **Available Tools** — Table of all 10 tools with descriptions and args
4. **Tool Details** — Each tool with examples:
   - `pr_review` — Review a PR
   - `codebase_query` — Query codebase architecture
   - `suggest_feature` — Get feature suggestions
   - `audit` — Run security/quality audit
   - `test_generator` — Generate test cases
   - `deployment_status` — Check deployment health
   - `sanity_data` — Query Sanity data (10 presets + raw GROQ)
   - `vercel_urls` — Get production/preview URLs
   - `vercel_logs` — Get runtime/build logs
   - `vercel_build_logs` — Get build events
5. **Common Workflows** — Step-by-step for typical tasks
6. **Quick Reference** — Cheat sheet table
7. **Boundaries** — When NOT to use this skill

### Conventions followed:
- Description starts with "Use when..." (trigger conditions only)
- Concise body (<500 words target)
- Table for quick scanning
- Code examples inline
- Cross-reference to related skills (github-pr-workflow, audit, etc.)

## Verification
1. Check file exists: `ls ~/.junie/skills/ylx-mcp/SKILL.md`
2. Validate YAML frontmatter parses correctly
3. Confirm skill appears in Junie's skill list
